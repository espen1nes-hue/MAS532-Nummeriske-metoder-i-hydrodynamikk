# MAS532

Prosjekt for MAS532.

## Innhold
- `src/` – Julia-kode (WaterLily)
- `results/` – genererte resultater (ikke versjonert)
- `figures/` – genererte figurer (ikke versjonert)

## Kjøring (eksempel)
Åpne Julia og kjør relevante filer i `src/`.
